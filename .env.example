# HMAC signing secret for session cookies (src/lib/server/auth/session.ts).
# Required in production; in dev, an insecure fallback is used with a warning if unset.
# Generate a real value with: openssl rand -base64 32
SESSION_SECRET=change-me-in-production
